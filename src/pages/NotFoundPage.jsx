import React from 'react';

const NotFoundPage = () => {
    return (
        <div>
            <h1>존재하지 않는 페이지 입니다.</h1>
            <h4>App.jsx 파일에서 Route를 설정해주세요.</h4>
        </div>
    );
};

export default NotFoundPage;
